[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "er"
version = "0.1.0"

[tool.setuptools.packages.find]
include = [
    "utils",
    "models",
    "moede",
    "VER",
    "MER",
    "TER",
    "data",
]
